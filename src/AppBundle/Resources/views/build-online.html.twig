{% extends 'AppBundle::base.html.twig' %}

{% block keywords_description %}
    <meta name="keywords" content="placeholder, image, create, online, tool">
    <meta name="description" content="Use our placeholder image creation tool to build your image online">
{% endblock %}

{% block title %}
    Placeholder building tool
{% endblock %}

{% block content %}
<div class="container">
    <div class="page-header">
        <h1>Create a placeholder image</h1>
        <p>Use our online tool to create your next placeholder image</p>
    </div>
    <div class="row">
        <div class="col-xs-12 col-sm-8">
            {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.text) }}
                <div class="col-sm-10">
                {{ form_widget(form.text) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.width) }}
                <div class="col-sm-10">
                {{ form_widget(form.width) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.height) }}
                <div class="col-sm-10">
                    {{ form_widget(form.height) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.bgcolor) }}
                <div class="col-sm-10">
                    <div class="input-group bgcolor">
                        {{ form_widget(form.bgcolor) }}
                        <span class="input-group-addon"><i></i></span>
                    </div>
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.fontcolor) }}
                <div class="col-sm-10">
                    <div class="input-group bgcolor">
                        {{ form_widget(form.fontcolor) }}
                        <span class="input-group-addon"><i></i></span>
                    </div>
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.fontsize) }}
                <div class="col-sm-10">
                    {{ form_widget(form.fontsize) }}
                </div>
            </div>
            <div class="form-group">
                <div class="col-xs-12 text-right">
                    <button
                            id="generate-placeholder"
                            class="btn btn-primary"
                            data-base-path="{{ url('get-placeholder') }}">Get Placeholder</button>
                </div>
            </div>
            {{ form_end(form) }}
            <h3>Url parameters definition</h3>
            <p>Let's go through with the url's parameters. There are 6 parameters in total none of which are required.</p>
            <h4>They are</h4>
            <ul class="list-group">
                <li class="list-group-item">text - text to be printed on the placeholder image. Should be url encoded.<span class="badge">default: empty</span></li>
                <li class="list-group-item">width - integer value of image width in pixels<span class="badge">default: 300</span></li>
                <li class="list-group-item">height - integer value of image height in pixels<span class="badge">default: 300</span></li>
                <li class="list-group-item">color - hex value of background color for the placeholder image. Omit the pound sign(#)<span class="badge">default: FFFFFF</span></li>
                <li class="list-group-item">font-color - hex value of the font's color. Omit the pound sign (#) as well<span class="badge">default: 555555</span></li>
                <li class="list-group-item">font-size - integer value in pixels for font's size<span class="badge">default: 14</span></li>
            </ul>

            <h3>Help us running</h3>
            <p>If you like our service, please, donate to help us run our project.</p>
            <div>
                <form action="https://www.paypal.com/cgi-bin/webscr" method="post" target="_top">
                    <input type="hidden" name="cmd" value="_s-xclick">
                    <input type="hidden" name="hosted_button_id" value="BMGKQJGTRACFY">
                    <input type="image" src="https://www.paypalobjects.com/en_US/i/btn/btn_donateCC_LG.gif" border="0" name="submit" alt="PayPal - The safer, easier way to pay online!">
                    <img alt="" border="0" src="https://www.paypalobjects.com/ru_RU/i/scr/pixel.gif" width="1" height="1">
                </form>
            </div>
        </div>
        <div class="col-xs-12 col-sm-4">
            <div class="well">
                <h2>Disclaimer</h2>
                <p>Let's get through with our placeholder creation tool.</p>
                <p>The form on the left is already filled with default values. Most of them are self explanatory. But we're going to make sure there are no questions left behind.</p>
                <h3>Text field</h3>
                <p>Use it to define the text to be printed on the placeholder image. Or just leave it blank.</p>
                <h3>Width and Height fields</h3>
                <p>These field are used to define the placeholder's size in pixels.</p>
                <h3>Background Color</h3>
                <p>Placeholder's background color. Use hex value to define it. You can enter it directly or click the right part of the input field (the greyed one) and use a little color picker popup.</p>
                <h3>Font Color and Size fields</h3>
                <p>Use these to define font's properties.</p>
                <h3>Push the button</h3>
                <p>After you are done with the parameters push the "Get Placeholder" button. You'll see a popup window with the image you create. In that window you can either copy and paste the url of the image or simply download the image itself.</p>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="placeholder-modal" tabindex="-1" role="dialog" aria-labelledby="placeholderModal" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel">That's your placeholder</h4>
            </div>
            <div class="modal-body">
                <div class="row">
                    <div class="col-xs-12 text-center">
                        <div class="thumbnail">
                            <img src="#">
                        </div>
                    </div>
                    <div class="col-xs-12">
                        <pre class="url-view"></pre>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <a href="#" class="btn btn-primary" id="download-image-btn">Download Image</a>
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('vendor/mjolnic-bootstrap-colorpicker/dist/js/bootstrap-colorpicker.min.js') }}"></script>
    <script>
        $(document).ready(function () {
            $('.bgcolor').colorpicker();

            $('#generate-placeholder').click(function(){

                var placeholder = {
                    text: $('#placeholder_text').val(),
                    width: $('#placeholder_width').val(),
                    height: $('#placeholder_height').val(),
                    color: $('#placeholder_bgcolor').val().replace('#', ''),
                    'font-color': $('#placeholder_fontcolor').val().replace('#', ''),
                    'font-size': $('#placeholder_fontsize').val()
                };

                var baseUrl = $(this).attr('data-base-path');

                var url = baseUrl + '?' + $.param(placeholder);
                var downloadUrl = baseUrl + '/file?' + $.param(placeholder);

                $("#placeholder-modal .thumbnail img").attr('src', url);
                $("#placeholder-modal pre.url-view").text(url);
                $("#download-image-btn").attr('href', downloadUrl);
                $("#placeholder-modal").modal();

                return false;
            });

        });
    </script>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('vendor/mjolnic-bootstrap-colorpicker/dist/css/bootstrap-colorpicker.min.css') }}">
{% endblock %}